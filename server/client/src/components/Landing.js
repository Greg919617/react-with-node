import React from 'react';

const Landing = () => {
    return (
        <div style = {{ textAlign: 'center'}}>
        <h1>
        emaily!
        </h1>
        Collect Feedback for your users
        </div>
    );
};

export default Landing;
